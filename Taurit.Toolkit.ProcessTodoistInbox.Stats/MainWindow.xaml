<Window
    x:Class="Taurit.Toolkit.ProcessTodoistInbox.Stats.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Taurit.Toolkit.ProcessTodoistInbox.Stats"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:wpf="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
    Title="Todoist Backlog Statistics"
    Width="800"
    Height="450"
    DataContext="{Binding RelativeSource={RelativeSource Self}}"
    ContentRendered="MainWindow_OnContentRendered"
    mc:Ignorable="d">
    <Grid x:Name="ChartWrapper" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="162*" />
            <RowDefinition Height="257*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Border Grid.RowSpan="2" Margin="10">
            <StackPanel>
                <TextBlock Margin="0,0,0,3">Chart type</TextBlock>

                <RadioButton Checked="RadioButtonSetupChanged" GroupName="ChartType">Total estimated time of tasks without due date</RadioButton>
                <RadioButton Checked="RadioButtonSetupChanged" GroupName="ChartType">Number of tasks without due date</RadioButton>

                <TextBlock Margin="0,10,0,3">Time period to analyze:</TextBlock>
                <RadioButton
                    Name="Time_LastWeek"
                    Checked="RadioButtonSetupChanged"
                    GroupName="TimePeriod"
                    Tag="7">
                    Last week
                </RadioButton>
                <RadioButton
                    Name="Time_LastMonth"
                    Checked="RadioButtonSetupChanged"
                    GroupName="TimePeriod"
                    IsChecked="True"
                    Tag="30">
                    Last month
                </RadioButton>
                <RadioButton
                    Name="Time_LastYear"
                    Checked="RadioButtonSetupChanged"
                    GroupName="TimePeriod"
                    Tag="365">
                    Last year
                </RadioButton>
                <RadioButton
                    Name="Time_AllTime"
                    Checked="RadioButtonSetupChanged"
                    GroupName="TimePeriod"
                    Tag="99999">
                    All time
                </RadioButton>


            </StackPanel>
        </Border>
        <wpf:CartesianChart
            Name="WeightChart"
            Grid.Row="0"
            Grid.RowSpan="2"
            Grid.Column="1"
            Background="White"
            DataTooltip="{x:Null}"
            Hoverable="False">
            <wpf:CartesianChart.AxisX>
                <wpf:Axis LabelFormatter="{Binding XFormatter}" />
            </wpf:CartesianChart.AxisX>
            <wpf:CartesianChart.Series>
                <wpf:LineSeries Values="{Binding YData}" />
            </wpf:CartesianChart.Series>
        </wpf:CartesianChart>
    </Grid>
</Window>